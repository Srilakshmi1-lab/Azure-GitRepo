client operating system and server operating system are two different operating systems.
kernal is main in the linux operating system it works on input devices in linux operating system.
if we want to work on multiple operating systems then we install virtual Box

